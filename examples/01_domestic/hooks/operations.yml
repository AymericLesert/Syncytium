# Les opérations du projet banque (D776/D777/D778)
# Le mapping nommé : code (le langage du domaine 7 — D570) + properties ;
# le fonctionnement décrit dans le md voisin (la doc auto-générée D645).
operations:
  clone:
    code: operations/clone
    properties: { }
  propagation:
    code: operations/propagation
    properties: { }
  virement:
    code: operations/virement
    properties: { }
  creer_releve:
    code: operations/creer_releve
    properties: { }
