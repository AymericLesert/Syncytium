# La phase 2 du rapprochement (D821) — rechercher et associer :
# la règle de complément re-parcourt les mêmes lignes et pose liee sur les
# écritures nées de 005 (la correspondance ligne→enregistrement tenue par la
# migration — D666/D668). Le FILO croise : la première ligne relue dépile la
# seconde empilée — son miroir, jamais elle-même ; la garde cache.size > 1
# écarte l'orphelin (il ne dépile pas — liee reste vide). L'affectation au
# chemin repose la référence sur le miroir (me.liee.liee : me — D823) : les
# deux côtés portés, l'uniformité avec virer ; le chemin sur le vide est sans
# effet (l'orpheline reste intacte).

budget:
  to: banque.ecriture
  filter: Budget != "OUVERTURE" and Budget != "FERMETURE"
  parent: { compte: Numero_Compte }   # le possesseur par sa clé fonctionnelle (D654)
  fields:
    liee: cache.pop("virement", reference_virement) if cache.size("virement", reference_virement) > 1
    me.liee.liee : me
